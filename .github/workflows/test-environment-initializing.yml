name: Test environment initializing

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  test:
    name: Test environment initializing on ${{ matrix.os }}
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        os: [fedora-latest, debian-latest, centos-latest]
        
    container:
      image: ${{ matrix.os }}
      
    steps:
    - uses: actions/checkout@v2
    
    - name: Run install script
      run: |
        chmod +x script.sh
        ./script.sh
        
    - name: Run tests
      run: |
        # Add your test commands here
        # For example: 
        # shellcheck script.sh